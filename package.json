{
  "name": "server-firebase",
  "version": "1.0.0",
  "description": "API server con Firebase Firestore como storage",
  "main": "server-firebase.js",
  "scripts": {
    "start": "node server-firebase.js",
    "dev": "nodemon server-firebase.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^4.22.2",
    "firebase-admin": "^12.7.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
